// import owner contrat and role from openZepplin

// declaration of the structure for a director / teacher / students

// role attributions 

// grade structure

// 2. the director of the school will add teacher
fonction registerTeacher(address _teacherAddress){
 // add teacher
};

// 3. the director of the school will add role add the teacher . 
fonction giveRoleForTeacher(address _teacherAddress){
 // add role for teacher
};

// 4. teacher will be able to add students.
fonction registerStudent(address _studentAddress, string _nameStudent){
 // add student
};

// 5. teacher will be able to add grade to students.
fonction addGrade(address _studentAddress, uint _grade){
 // add grade
};

fonction getGradeFromStudent(address _studentAddress){
 // get grade from student
};